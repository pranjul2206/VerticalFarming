<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.verticalfarming">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".mapwoaah"></activity>

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_key" />

        <activity android:name=".FliprBackgroundRun" />
        <activity android:name=".CropHandholdingContent" />
        <activity android:name=".CityGreensUniversity" />
        <activity android:name=".GenericEducationContent" />
        <activity android:name=".SupportCenter" />
        <activity android:name=".CropHandholding" />
        <activity android:name=".GenericEducation" />
        <activity android:name=".newsArea" />
        <activity
            android:name=".BasicHome"
            android:label="@string/title_activity_basic_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- alll services here -->
        <service android:name=".Myservice" />
    </application>

</manifest>